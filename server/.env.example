# Copy to .env and set your API token (never commit real tokens)
THENEWSAPI_TOKEN=your_token_here
PORT=5177
# When using search, restrict results to the last N days (default 30). Set to 0 to disable.
SEARCH_RECENCY_DAYS=30
